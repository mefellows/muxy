package muxy

// Test fixture

// For example, allow several calls to pass through before stuffing around

// Added random variation into the mix - e.g. Apply a symptom on a normally distributed pattern, skewed pattern etc.

// Default is 100% probability of a pattern
